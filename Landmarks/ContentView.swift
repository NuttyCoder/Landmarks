//
//  ContentView.swift
//  Landmarks
//
//  Created by Heather Scheer on 8/17/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("this is the new landmarks!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
